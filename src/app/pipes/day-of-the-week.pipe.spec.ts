import { DayOfTheWeekPipe } from './day-of-the-week.pipe';

describe('DayOfTheWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new DayOfTheWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
